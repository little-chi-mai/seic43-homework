consol.log( "Priyanka");
//
/*  const squareNumber = function (number){
  result = number * number;
  return result;
  console.log (`The result of squaring the ${ number } is ${ result }`)
}

squareNumber(3);

  const halfNumber = function (number) {
  result = number / 2;
  return result;
  console.log (`Half of ${ number } is $ { result }`);
}

halfNumber(5);

  const percentageOf = function (num1, num2, percentage = 50%) {
  num2 =  (num1 / 100) * percentage
  console.log (`${ num2 } is ${ percentage} % of ${ num1})`);
}

percentageOf (4, 8);

  const areaOfCircle = function (radius) {
  area = Math.PI * (radius ^ 2);
  return area;
  console.log(`The area of circle with radius ${ radius } is Math.ceil ${ area }`)
}

areaOfCircle(6);

  Const grandTotal = function (number) {
  const halfOfNum = halfNumber(number);
  const squareOfNum = halfOfNum ^ 2;
  const calculateArea =areaOfCircle(squareOfNum);
  percentageOf (calculateArea, squareOfNum);

  console.log();
} */
